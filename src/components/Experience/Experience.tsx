import React from 'react';
import withPage from "../Page/Page";

const Experience = () => {
    return (
        <div>
            Experience
        </div>
    );
};

export default withPage(Experience);